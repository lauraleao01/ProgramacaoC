#include <stdio.h>

int main() {
  int temperatura;
    printf("Digite a temperatura atual: ");
    scanf("%d", &temperatura);

temperatura > 30 ? printf("Está calor\n") : printf("Frio\n");

  return 0;
}